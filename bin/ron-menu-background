#!/usr/bin/env fish

set dir $argv[1]
set -q dir[1] || set dir ~/pictures/backgrounds

set selected (swayimg $dir -g -c ~/.config/swayimg/backgrounds.lua)

if test -n "$selected"
    ron-background-set $selected
end
