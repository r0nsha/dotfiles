#!/usr/bin/env fish
# Toggle mount/unmount for a udisks device

set device $argv[1]
if test -z "$device"
    exit 1
end

if test (udiskie-info $device -o "{is_mounted}") = True
    udiskie-umount "$device"
else
    udiskie-mount "$device"
end
